{
  "name": "silverstripe-privacy-embed",
  "description": "",
  "contributors": [
    {
        "name": "wake up page design",
        "url": "https://wake.de"
    },
    {
        "name": "Jan Metzger",
        "url": "https://github.com/Zazama"
    }
  ],
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "build": "yarn && NODE_ENV=production webpack --mode production --bail --progress",
    "dev": "NODE_ENV=development webpack --progress",
    "watch": "NODE_ENV=development webpack --watch --progress"
  },
  "dependencies": {
    "react": "^18.2"
  },
  "devDependencies": {
    "@silverstripe/eslint-config": "1.0.0-alpha6",
    "@silverstripe/webpack-config": "2.0.0-alpha9",
    "webpack-cli": "^5.0.1"
  },
  "babel": {
    "presets": [
      [
        "@babel/preset-env",
        {
          "modules": false
        }
      ],
      "@babel/preset-react"
    ]
  }
}
